package game;

import slayter.display.Sprite;

class Game extends Sprite {
	public function new(width:Int, height:Int):Void {
		super();
		this.init(width, height);
	}

	private function init(width:Int, height:Int) {
	}
}
